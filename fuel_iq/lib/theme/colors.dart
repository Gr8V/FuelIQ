import 'package:flutter/material.dart';

class AppColors {
  // Light theme
  static const lightScaffold = Color(0xFFF0F2F5);   // page background
  static const lightCard = Color(0xFFFFFFFF);       // card background
  static const lightPrimary = Color(0xFF2E7D32);    // primary green
  static const lightSecondary = Color(0xFFFF9800);  // secondary orange
  static const lightTertiary = Color(0xFF009688);   // accent teal
  static const lightTextPrimary = Color(0xFF212121);
  static const lightTextSecondary = Color(0xFF616161);
  static const lightError = Color(0xFFB00020);

  // Dark theme
  static const darkScaffold = Color(0xFF121212);
  static const darkCard = Color(0xFF1E1E1E);
  static const darkPrimary = Color(0xFF00C853);
  static const darkSecondary = Color(0xFFFFB300);
  static const darkTertiary = Color(0xFF26A69A);
  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFFB0B0B0);
  static const darkError = Color(0xFFFFB4AB);

  // Macro colors
  static const proteinColor = Color(0xFFD32F2F); // red
  static const carbsColor = Color(0xFFFFB300);   // amber
  static const fatColor = Color(0xFF1976D2);     // blue
}
